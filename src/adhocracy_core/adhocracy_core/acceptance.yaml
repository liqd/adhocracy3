# test configuration for acceptance tests

configurator:
  zodbconn:
    uri: 'zeo://localhost:9999?cache_size=200MB&connection_cache_size=50000&storage=main&blob_dir=var/db/test/blobs&shared_blob_dir=True'
  substanced:
    secret: 'secret'

adhocracy:
  adhocracy.use_mail_queue: True
  ws_url: 'ws://localhost:8081'
  canonical_url: 'http://localhost:9090/static/embed.html#!'
  frontend_url: 'http://localhost:9090'
  use_mail_queue: True
